{
  "name": "jarvis",
  "version": "0.1.0",
  "description": "A persistent AI vessel with tiered context engineering",
  "type": "module",
  "private": true,
  "bin": {
    "jarvis": "src/cli-entry.ts"
  },
  "scripts": {
    "check": "bun run --bun tsc --noEmit",
    "test": "bun test",
    "dev": "bun run src/daemon.ts",
    "start": "bun run src/cli-entry.ts"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "0.78.0",
    "eventsource-parser": "3.0.6"
  },
  "devDependencies": {
    "@types/bun": "1.3.9",
    "typescript": "^5"
  }
}
